
import { useAudioRecorder } from '@/hooks/recording-utils';
import type { MusicTrackType, UseRecorderOptions } from '@/hooks/recording-utils';

export { MusicTrackType };
export const useRecorder = useAudioRecorder;
